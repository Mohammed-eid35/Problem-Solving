//:::: Alien :::://
// Muhammad Eid //
#include <bits/stdc++.h>
using namespace std;

void Mo35() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
}

int main() {  Mo35();

    char s; cin >> s;
    int n; cin >> n;
    while(n--) {
        int x; cin >> x;
        while(x--)
            cout << s;
        cout << '\n';
    }

return 0;
}
