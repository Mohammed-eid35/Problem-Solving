//:::: Alien :::://
// Muhammad Eid //
#include <bits/stdc++.h>
using namespace std;

void Mo35() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
}

int main() {  Mo35();

    long long x, y; cin >> x >> y;
    cout << x << " + " << y << " = " << (x + y) << '\n';
    cout << x << " * " << y << " = " << (x * y) << '\n';
    cout << x << " - " << y << " = " << (x - y) << '\n';

return 0;
}
