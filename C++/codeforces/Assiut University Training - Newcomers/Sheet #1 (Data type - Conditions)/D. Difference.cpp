//:::: Alien :::://
// Muhammad Eid //
#include <bits/stdc++.h>
using namespace std;

void Mo35() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
}

int main() {  Mo35();

    long long a, b, c, d; cin >> a >> b >> c >> d;
    cout << "Difference = " << (a * b) - (c * d);

return 0;
}
